{
  "applinks": {
    "details": [
      {
        "appIDs": ["4G55Z778KC.com.niugio.listr"],
        "components": [
          { "/": "/join/", "comment": "The NEWER shape is /join/?c=<CODE>, whose path is bare /join/ with the code in the query. Claimed here ahead of use: as of 2026-08-21 nothing emits it - buildInviteLink in the app still returns /join/<CODE> - so this entry is the site being ready, not a description of links in circulation. Stated explicitly rather than left to /join/* below, because whether that wildcard matches ZERO characters is the sort of thing that is far cheaper to write down than to test: an AASA is fetched by Apple, cached on the device, and cannot be verified from a laptop at all. A component with no ? key matches ANY query string, so this one entry covers every code, and a bare /join/ with no code still opens the app - which is the right outcome, since the app has its own empty state for that and a dead-end web page is not an improvement on it." },
          { "/": "/join/*", "comment": "The ORIGINAL shape, /join/<CODE>. Kept forever: invite links live in people's message history, not in a deploy, and nothing shipped here can reach the ones already sent." },
          { "/": "/de/join/", "comment": "The German invite link in the newer shape, /de/join/?c=<CODE>. Same reasoning as /join/ above, including that nothing emits it yet." },
          { "/": "/de/join/*", "comment": "The German invite path, original shape. Mirrors app.json's Android intentFilters, which already list both prefixes - and note those need no change for the query form, because an Android pathPrefix matches the PATH only and /join already covers /join/?c=<CODE>." }
        ]
      }
    ]
  }
}
